export const selectCourtList = state => state.court.courtsList;

export const selectCurrentCourt = state => state.court.currentCourt;

export const selectPostalCode = state => state.court.postalCode;

export const selectSimcId = state => state.court.simcId;

export const selectStreet = state => state.court.street;

export const selectTercId = state => state.court.tercId;

export const selectIsLoading = state => state.court.isLoading;

export const selectError = state => state.court.error;